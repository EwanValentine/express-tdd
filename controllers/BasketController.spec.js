const sinon = require('sinon')
const chai = require('chai')
const expect = chai.expect

const invalidSKU = 'Invalid SKU'

const BasketController = require('./BasketController')

describe('BasketController', () => {
  it('should return a json response with a basket total for a valud SKU', () => {
    
  })

  it('should return a json response with an error for an invalid SKU', () => {

  })
})
